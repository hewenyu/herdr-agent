# 在飞书中用自然语言管理 herdr 任务

核查日期：2026-09-16。

本项目的 AI 入口基于 [Eino](ai-framework.md)，运行在 Go 服务中，使用用户配置的模型 API、模型名称和 API key。在现有飞书应用机器人的私聊里直接描述需求，即可查询项目、创建开发任务、查看进展、补充要求、完成或销毁会话。例如：“在 herdr-agent 项目里排查登录失败的问题”“目前有哪些任务正在进行”“给刚才那个任务补充一个要求”。日常操作不需要记住 `/new` 等命令。

Pi 的相关框架包使用 TypeScript；当前实现采用纯 Go，保留自然语言理解和受控工具调用的能力目标，不依赖 Pi 或 Node.js。个人飞书助手的 MCP 接入是另一条已调研的候选路线，见本文末尾；当前入口使用本项目自己的机器人。

## 调用关系

```text
用户在本项目飞书机器人私聊中描述需求
  → 本项目检查飞书消息发送者的身份与允许名单
  → 配置的模型理解需求，调用本项目提供的任务工具
  → 本项目按操作者身份和项目配置管理 herdr 会话与飞书任务
  → 工具返回实际状态、最近回复和任务链接
  → 本项目根据真实工具结果渲染操作回执和进度
```

一个项目可关联多个本地目录，在本机配置页面管理。AI 选择已经配置的项目名称和执行 agent；只有用户明确要求新建项目时才创建目录，不接受任意本地路径、终端窗口或操作者身份作为工具参数。模型只负责任务理解与工具选择；本机 Codex 或 Claude 继续负责执行开发任务。

入口私聊用于创建任务和查询总览，每个任务群始终绑定一个任务。群内自然语言经受限任务助手处理：进度查询读取当前任务，需求和反馈发送给对应 coding agent，明确验收结单则完成并关闭任务会话。无需 @ 或斜杠命令，启动确认、审批和主动进展也留在群里。群工具不能切换任务或新建项目。AI 出错时返回固定提示，不会把同一段文字误发给终端；工具操作可能已经发生，需查询实际状态确认。

进度和工具回执使用实际数据渲染，不信任历史 AI 回复中的状态、文件名或验收结论。模型每轮获得新的任务快照。首次要求未送达、执行错误、同步中等状态必须显示；agent 最近回复标记为自述，目录条目只代表当前文件系统现况，不等于该任务已生成并验证产物。

## 配置与使用

沿用本项目已经完成授权的飞书应用，无需飞书 CLI，也无需给云端个人助手发布一个可访问本机的 MCP 地址。具体配置字段、API 协议和示例以 [README 的自然语言任务入口](../README.md#control-tasks-with-natural-language)为准。

1. 启用任务管理，确认当前用户在本项目允许名单中。在本机配置页面设置项目名、一个或多个目录及默认 Codex 或 Claude，也可以稍后明确要求 AI 创建新项目。
2. 启用 AI 入口，设置模型服务的协议、API base URL、模型名称及请求超时。选用支持工具调用的模型。
3. 将模型服务的 key 写入环境变量或本项目 `.env` 的 `HERDR_AGENT_AI_API_KEY`，然后重启 `herdr-agent serve`。模型 key 和飞书 App Secret 分别使用各自的配置字段。
4. 打开本项目机器人的私聊创建任务或查询总览，再进入对应任务群沟通实现细节、询问进度和验收结单。

自然语言消息、相关任务摘要及工具结果会发送到配置的模型服务。任务执行仍使用本机既有的 Codex/Claude 环境；入口模型和实际执行开发的 agent 可分别配置。

## 本地项目配置

`herdr-agent serve` 默认同时提供 **http://127.0.0.1:18790/**，前端页面和 API 都由 Go 服务提供。
尚未启动桥时可运行 `herdr-agent configure --open` 单独打开页面；它与 `serve` 共用实例锁。
桥已运行时直接打开现有页面。`serve --config-listen 127.0.0.1:18791` 可更换地址，
`serve --no-config-ui` 可关闭页面。

页面支持一个项目配置多个文件夹、选择默认项目和 agent。第一个目录作为 agent 工作目录，其余目录通过
`--add-dir` 传入。保存或启动时主目录自动初始化 Git，已有有效仓库和 worktree 保留；附加目录不要求 Git。
本机需安装 Git。目录不存在时不能保存或启动新任务。已配置目录被移走
不会阻止页面启动，可在页面修复路径。已有任务保留创建时的目录快照，修改后的项目配置立即用于新任务。

全局 Bypass 开关默认开启，新 agent 分别使用 Codex 的 `--dangerously-bypass-approvals-and-sandbox`
或 Claude 的 `--dangerously-skip-permissions`，跳过常规执行审批。关闭后，新 agent 使用正常权限处理。
运行中的任务保留原模式。首次保存目录配置前可在 TOML 设置 `tasks.bypass = false`，后续页面保存优先。

首次保存后，完整项目列表、默认项目和 Bypass 开关写入 `~/.herdr-agent/projects.json`，文件权限 0600。
这个文件是后续项目配置来源，TOML 项目表只负责初始导入；`tasks.enabled` 与轮询间隔仍由 TOML 控制。
项目变更不需要重启。页面不编辑模型 API key，模型配置仍使用 `[ai]` 和 `HERDR_AGENT_AI_API_KEY`。

“在已有项目里新建任务”只使用配置目录。“新建一个 demo-api 项目，实现健康检查接口”才允许 AI 通过
`herdr_create` 的 `new_project=true` 显式创建 `~/herder-agent-code/demo-api/`，并登记为可复用项目。
这里的 `~` 属于运行 Go 程序的操作系统用户，不按飞书用户另建 home。主目录自动执行 `git init`，
不自动提交或创建 worktree；同名目录已存在时拒绝接管。删除项目配置保留目录和已有任务。

## 如何验证

先在机器人私聊发送只读请求：

> 看看可以操作哪些项目，再告诉我现在有哪些任务在进行。

回复应包含本项目配置中的项目和当前用户的实际任务状态。模型不可达、工具调用失败或没有任务时，应如实说明，不能将聊天记忆或示例数据当成实时查询结果。

查询通过后，再发送：

> 在 herdr-agent 项目创建一个测试任务，让 Codex 不修改任何文件，只回复 FEISHU_AI_OK。

检查以下结果：

- 飞书任务与独立任务群已创建，任务链接可以打开。
- 本机 herdr 创建了对应会话，agent 执行了测试要求，回复写回飞书。
- 在任务群直接问“现在任务进度如何”，得到当前绑定任务的实际阶段、目录现况、最近回复和更新时间。
- 直接在任务群补充要求，消息到达该任务绑定的 agent。
- agent 一轮回复结束后显示待验收或等待下一步；明确完成操作才标记飞书任务完成。
- 在飞书任务面板完成或重开任务后，再次查询能看到同步后的状态。
- 说“验收通过，可以结单”后，飞书完成状态得到确认、结果保存、群内收到关闭通知，再清理会话和群；代码保留。
- 说“没有验收，不要关闭”不应触发关闭。只勾选完成或明确“保留群”时会话继续保留。

创建和销毁会产生真实资源变化，使用单独测试任务核对。只通过本地测试不能算作模型 API、飞书应用与本机 agent 的端到端验证通过。

## 当前验证记录与状态总结

任务层已使用**本项目自己的飞书应用**完成真实联调：创建飞书任务和任务群、启动 Codex、回复与状态回写、完成、重开、销毁，以及任务事件订阅调用。测试任务记录保留在测试租户内。

自然语言入口已完成真实 Responses 和 Anthropic Messages 的只读工具验收：模型实际调用项目查询和任务查询，并按本地状态返回结果。本次新增目录数量、Bypass 状态和 `new_project` 参数后，两种协议的完整 9 个工具定义再次被真实服务接受，查询返回了实际项目与 Bypass 状态。这是 schema 与只读调用验收，不代表模型已经真实执行新建项目。

2026-09-16，本地配置页面通过真实 Chrome 操作验收：多目录增删排序、切换 agent、默认项目、Bypass 保存及刷新、新项目目录创建、删除配置后保留目录；桌面与手机布局正常。Go HTTP API 到项目目录库、任务持久化、启动参数与任务提示的集成测试通过，包含配置变更后旧任务仍使用原快照、未确认启动不自动投递任务、启动前目录失效可以纠正后重试。

同日，Codex 和 Claude Code 均通过本项目的 Go 客户端在真实 herdr 中启动，启用各自原生 Bypass 参数并传入两个附加目录。两个 agent 分别读取含空格、单引号路径中的未知随机内容并写出证明文件，四份结果独立逐字核对一致；所有测试 pane 已关闭。该验收只操作隔离测试目录，没有创建飞书消息或长连接。记录在本地忽略目录 `build/project-livecheck/report.md` 与 `build/project-ui-check/result.json`。

本次群交互修复使用真实配置的 Responses 模型，通过 7 项隔离验收：私聊空目录进度、群进度、需求反馈、验收结单、未验收否定、未来条件和完成但保留群。飞书及终端操作使用替身，验证模型选择和真实回执输出，未向真实群发送测试消息。

真实 herdr 验收同时覆盖 Manager → Controller → Codex/Claude 的初始任务投递。两个 agent 均仅投递一次，投递回读确认成功，并在配置主目录内和附加目录写入逐字匹配随机输入的证明文件；测试 pane 已关闭。长提示超出可见屏幕时使用本轮持久化随机标识确认回显，回执不代表代码完成或验收通过。记录在本地忽略目录 `build/task-output-livecheck/report-trusted.md`。

新 Git 目录首次启动仍可能出现 Codex/Claude 原生目录信任确认，即使 Bypass 已开启。系统保持“要求尚未发送”，在确认后继续；有编号的选择可使用群卡片，没有可选按钮时需在本机 herdr 窗口确认一次。不会将等待确认报告成已执行。

**尚未完成修复后的真实用户飞书消息 → AI 创建 → agent 产物 → 群内验收结单的完整现场验证。** 上述分层验收不能合称为这项端到端验证通过。

项目、执行阶段、阻塞原因、更新时间和最近回复会写入正式飞书任务数据。本项目 AI 入口通过任务查询工具直接读取这些状态，因此能够回答哪些任务正在进行、哪些等待验收、哪些遇到阻塞。具备任务访问能力的其他飞书 AI 也可以利用任务内容总结，但本项目写入任务数据并不代表所有飞书 AI 都已接入，外部搜索索引也可能存在更新延迟。

验收结单通过 `herdr_close` 串联完成、确认、保存结果和关闭通知，再清理执行会话与群；任一步失败都保留可恢复状态。只标记完成或只销毁仍是独立操作。执行 agent 一轮结束并不等于用户已验收；状态回复报告待处理操作和同步错误。

## 调研背景：个人飞书助手与 MCP

飞书[专属工作伙伴说明](https://aily.feishu.cn/hc/1u7kleqg/jsmwydi4)介绍了可自定义名字和技能的个人 AI 助手；[更名说明](https://www.feishu.cn/hc/zh-CN/articles/790732948604)注明飞书 aily 于 2026 年 8 月 14 日更名为“豆包工作伙伴”。仅凭联系人显示名称，不能确定账号当前可用的产品能力。

官方[添加和使用 MCP](https://aily.feishu.cn/hc/1u7kleqg/fiogabrc)文档说明个人助手和自定义智能体可以添加 MCP，支持 SSE、Streamable HTTP、自定义请求头及个人可用范围；[技能说明](https://aily.feishu.cn/hc/1u7kleqg/zja0qy5v)还描述了上传技能文件的能力。云端助手要操作本机，需要可达的服务和身份验证；上传一份技能说明不会自动获得本机访问能力。

[企业工作伙伴 MCP 说明](https://aily.feishu.cn/hc/1u7kleqg/73ahi33a)提供可选用户身份签名，但不能据此认定个人助手界面也支持同样配置。本次未直接核验用户个人助手的管理页，也没有验证个人助手到本项目的 MCP 链路；它不是当前机器人 AI 入口的配置前提。

飞书另有原生任务智能体注册、主页更新和执行步骤写入接口，可在[官方公开 schema](https://github.com/larksuite/cli/blob/main/internal/registry/catalog/services/task.json)核对。这些接口描述智能体登记与执行记录，本身不证明个人助手会自动发现或调用本项目，也不替代任务执行入口。
